import { YoutubeurlDirective } from './youtubeurl.directive';

describe('YoutubeurlDirective', () => {
  it('should create an instance', () => {
    const directive = new YoutubeurlDirective();
    expect(directive).toBeTruthy();
  });
});
